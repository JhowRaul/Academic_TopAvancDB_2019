# Academic_TopAvancDB_2019
Mensagens com banco de dados Redis em Django - Python

```
pip install django
pip install gxredis
pip install nose
```

Baixar redis, executar o servidor, instalar os pacotes acima e BAZINGA.